type: custom:grid-layout
title: kitchen
path: kitchen
layout: !include /config/dashboards/templates/includes/hemma_screen_layout.yaml
cards:
  # ROOM HERO CARD
  - type: custom:button-card
    template: hemma_room
    name: Kitchen
    variables:
      image: kitchen
      image_position: center center

      show_weather: true
      weather_mode: room
      temp_unit: "F"

      # Presence from FP2 zones — shown as a badge in the presence row
      occupancy_sensor_1: binary_sensor.cooking_area_presence
      occupancy_label_1: Cooking Area
      occupancy_sensor_2: binary_sensor.island_sitting_area_presence
      occupancy_label_2: Island Sitting Area

  # MOBILE NAVBAR
  - !include /config/dashboards/templates/includes/hemma_navbar_mobile.yaml

  # ENTITY CARDS
  - type: custom:layout-card
    class: hemma-entities
    layout_type: custom:grid-layout
    layout: !include /config/dashboards/templates/includes/hemma_entity_layout.yaml
    cards:
      # Kitchen Island Lights (Matter switch)
      - type: custom:button-card
        template: hemma_light
        entity: switch.kitchen_island_lights
        name: Island Lights
        variables:
          icon: light

      # Kitchen Speaker (Google Cast)
      - type: custom:button-card
        template: hemma_media
        entity: media_player.kitchen_speaker
        name: Kitchen Speaker
        variables:
          icon: speaker

      - type: custom:button-card
        template: hemma_vacuum
        entity: vacuum.cleaning_lady
        name: Clean Kitchen
        variables:
          room: kitchen

